{
  "name": "wavis-gui-e2e-tooling",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Dev-only harness for launching and driving the Wavis Tauri desktop app. Not part of the shipped app; isolated package.json so it never touches clients/wavis-gui's own dependency tree.",
  "scripts": {
    "build:app": "node build-app.mjs",
    "test": "node global-setup.mjs && node --test --test-force-exit --test-concurrency=1 tests/**/*.spec.mjs"
  },
  "dependencies": {
    "@livekit/rtc-node": "^0.13",
    "webdriverio": "^9"
  },
  "devDependencies": {
    "@wdio/native-utils": "^2.5.0",
    "@wdio/tauri-service": "^1.2.0"
  },
  "overrides": {
    "@wdio/tauri-service": {
      "@wdio/native-utils": "^2.5.0"
    }
  }
}
